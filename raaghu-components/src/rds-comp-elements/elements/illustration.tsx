import React from "react";
import { RdsIllustration } from "../../rds-elements";

export const code_actual = () => {
    return (
        <RdsIllustration
            colorVariant="light"
            label="Currently you don't have any data"
            subLabel="Click on the button above to add data"
        />
    );
};

export default code_actual;
