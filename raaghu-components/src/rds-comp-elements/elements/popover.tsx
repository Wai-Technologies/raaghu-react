import React from "react";
import { RdsPopover } from "../../rds-elements";

export const code_actual = () => {
    return <RdsPopover popoverPosition="top" children={<p>Popover</p>} />;
};

export default code_actual;
