import React from "react";
import { RdsBigNumber } from "../../rds-elements";

export const code_actual = () => {
    return (
        <RdsBigNumber
            subTitleColorVariant="primary"
            subTitle="Visitors"
            bigNumber="2,236"
            children={<></>}
        />
    );
};

export default code_actual;
