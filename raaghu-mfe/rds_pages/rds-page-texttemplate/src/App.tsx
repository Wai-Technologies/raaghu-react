import React, { Suspense } from "react";
import TextTemplate from "./texttemplate/text-template";

const App = () => (
    <Suspense>
        <TextTemplate></TextTemplate>
    </Suspense>
);

export default App;
