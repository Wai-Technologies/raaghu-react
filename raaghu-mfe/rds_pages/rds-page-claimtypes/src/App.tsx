import React, { Suspense } from "react";

import ClaimTypes from "./Claim-Types/Claim-Type";

const App = () => (
    <Suspense>
        <ClaimTypes></ClaimTypes>
    </Suspense>
);

export default App;
